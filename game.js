
Gemini
チャットを新規作成
チャットを検索
画像
動画
ライブラリ
ノートブックを新規作成
NIST Computer Security Resource Center Glossary
Official Guide to the Japan Sumo Association
すべてのノートブック
30年前のゲームをGeminiで再現
Netlifyサイトのインサイト確認方法
緑の矢印の確認と質問
Netlifyで無料ウェブサイト作成
VSCode コード整形ショートカットキー
ホームページ制作の意義と進め方
南蛮漬けに生姜は必要か
出町柳駅周辺の墓地探し
スキルを活かすためのロードマップ
index.html 画像追加の相談
サイト制作、一緒に進めよう
今日の株価下落、市場の懸念
AIの貢献と喜び
AI活用術：実務効率化の相棒
キューバの深刻な経済危機とエネルギー不足
相撲 満員御礼 画像生成
相撲サイト発展アイデア集
面白い飲食店サイトの探し方
Geminiコーディング利用者の日平均規模
一方的に話す人の心理と対処法
「デブ」の語源、三つの説
Cursorが三大AIに入らない理由
HTMLからメニューと値段抽出
版画風画像生成指示
看板デザイン依頼と生成
版画風画像生成指示
画像生成AI修正ループ脱出法
サイト制作に関する質問と回答
今日の株価見通しと個別銘柄
CSSでスノーフォール実装とJS比較
共有相手の特定
家系図作成のための戸籍取得方法
スプレッドシートとHTMLのAPI連携
中学英語学習サイトのHTML/CSS生成
Gemini との会話
geminiはwebサイトでゲーム作ってくれたけど、何種類くらい作れるの？30年前くらいに有名だったやつとか

30年前（1990年代半ば）のゲーム、懐かしいですね！当時はスーパーファミコンの後期から、初代プレイステーションやセガサターン、アーケードゲーム（ゲームセンター）が全盛期だった時代です。

GeminiはWebブラウザ上で動くゲーム（HTML/CSS/JavaScript）をコード化して出力できるので、2D（平面）のレトロゲームやパズル、シンプルなアクションなら、ほぼ何種類でも再現可能です。

具体的に「30年前（1996年前後）に有名だったゲーム」をベースに、Geminiがサクッと作れる代表的なジャンルとタイトルをまとめました。

Geminiで再現・作成できる30年前の有名ゲーム
ジャンル	当時の代表作・イメージ	Geminiで作れる仕様
落ち物パズル	ぷよぷよ、テトリス、パネルでポン	ブロックの落下、回転、同じ色を4つ並べて消す連鎖の判定など。
固定画面シューティング	インベーダー、ギャラガ、ツインビー	自機を左右に動かし、迫る敵を撃つ。パワーアップカプセルのドロップなども実装可能。
2Dアクション・迷路	パックマン、ボンバーマン	壁の配置、敵キャラのシンプルな巡回AI、アイテムによる爆発範囲の拡大など。
ブロック崩し・対戦	アルカノイド、PONG（ポン）	バーを動かしてボールを打ち返す。ブロックの耐久値変化や、2人対戦用の簡易AI。
簡易RPG・ローグライク	風来のシレン、トルネコの大冒険	ターン制で動くダンジョンRPG。ランダムマップ生成は少し工夫がいりますが、固定マップなら一瞬です。
育成・シミュレーション	初代たまごっち（1996年発売）	時間経過でお腹が減る、ボタンでエサをあげる、ステータスに応じて進化するシステム。
なぜ作れるの？
30年前のゲームの多くは、ゲームのルール（ロジック）が非常にシンプルで洗練されているからです。
現在のWeb標準技術（特にHTML5のCanvasという描画機能とJavaScript）を使えば、当時のゲーム機以上の処理がブラウザだけで軽々と動かせます。

逆に、Geminiが「苦手」な30年前のゲーム
1996年頃といえば『バイオハザード』や『スーパーマリオ64』など、3Dグラフィックが登場し始めた時期です。

苦手なもの： 複雑な3D空間を動き回るゲーム（ポリゴンの格闘ゲームや、3Dダンジョンなど）

理由：コードの量が膨大になり、Webブラウザ用のコード1発では出力しきれないため。

作ってみたいときは？
「30年前に流行った『ぷよぷよ』みたいな落ち物パズルを、ブラウザで遊べる1ファイルのHTMLコードで作って。色を4つ繋げたら消えるルールで！」

というように、当時のタイトルやルールを具体的に指定して頼むと、その場ですぐに遊べるコードを書いてくれます。何か思い出のゲームはありますか？

思い出のゲームは幻想水滸伝とかアークザラットとかファイナルファンタジータクティクスだね

うわ、めちゃくちゃ渋くて最高の名作ばかりですね！どれもプレイステーション初期（1995〜1997年頃）を代表する、ストーリーもシステムも超一級品のRPG・シミュレーションRPG（SRPG）です。

正直にお伝えすると、これらの作品を「まるごと1本そのまま再現する」のはGeminiでも1発では難しいです。というのも、どれも重厚なシナリオ、膨大なドット絵、そして奥深い戦闘システムが組み合わさっているからです。

ですが、「あのゲームの、このシステム（戦闘画面など）だけを切り取って、ブラウザで遊べるミニゲームにする」 という形なら、Geminiで今すぐ作れます！

具体的にどんな風に再現できるか、3つの作品ごとにまとめてみました。

1. ファイナルファンタジータクティクス（FFT）風
【再現できる部分：クォータービュー（斜め見下ろし）のターン制バトル】

仕様： チェス盤のようなマス目の上で、味方（青）と敵（赤）を交互に動かすミニSRPG。

Geminiができること：

移動範囲の表示

「たたかう」を選んで隣のマス目の敵を攻撃するシステム

スピード（Act Time）順にターンが回ってくる簡易的な行動順リストの表示

2. アークザラッド風
【再現できる部分：エリア移動なしのシームレスなバトル】

仕様： 画面の切り替えがなく、フィールド上にそのまま敵と味方が配置されていて、近づいて殴り合うスピード感のあるバトル。

Geminiができること：

十字キー（または画面クリック）でキャラを動かし、敵に近づいて攻撃ボタンを押すとダメージを与えるアクションRPG風のバトル。

アーク風の「ちょっと引いた視点のドット絵風ステージ」を簡易的に再現。

3. 幻想水滸伝風
【再現できる部分：6人編成のターン制コマンドバトル ＆ 本拠地・仲間集め】

仕様： 前衛3人・後衛3人にキャラクターを配置して、全員の行動を最初に入力してから一斉に動き出すバトル。

Geminiができること：

おなじみの「たたかう」「防御」「紋章（魔法）」のコマンド選択。

「ボタンを押すと仲間がランダムで1人増えて、拠点がだんだん賑やかになっていくボタン」みたいな、簡易的な本拠地システムの再現。

もし「ちょっと遊んでみたい」と思ったら…
まずは一番シンプルな「アークザラッドやFFTみたいな、マス目の上を歩いて敵をポカポカ叩く、シンプルなターン制シミュレーションバトル」あたりからコードを作ってみるのがおすすめです。グラフィックはシンプルな色分けされた四角（■）になりますが、ゲームのルールはしっかり動くものが作れます。

どれか「このバトルの雰囲気をちょっとブラウザで動かしてみたい！」というものはありますか？

そこまで頑張りたくない。インベーダーみたいな超絶短期のゲームをたくさん持ってるサイト作れないか？と思って

なるほど、そっちの方向ですね！めちゃくちゃ分かります。凝ったRPGのシステムを1から作っていくのは骨が折れますし、ブラウザで遊ぶなら「アクセスして、10秒でルールが分かって、3分でサクッと終わる」ゲームが並んでいる方が圧倒的に楽しいです。

結論から言うと、インベーダーのような超絶短期ゲーム（ミニゲーム）を4〜5種類詰め込んだ「自分専用のゲームセンターサイト」は、Geminiを使えば1ファイル（HTML）だけで簡単に作れます！

トップ画面にゲームの選択ボタン（「インベーダー」「ブロック崩し」「テトリス」など）が並んでいて、クリックするとその場ですぐに切り替わって遊べる仕様です。

例えば、以下のようなラインナップを1つのサイトにまとめることができます。

1ファイルに詰め込める「超絶短期ゲーム」の例
スペースインベーダー風（シューティング）

左右移動とスペースキーでの弾発射だけで、迫る敵を全滅させるゲーム。

ブロック崩し（アクション）

マウスや指でバーを動かしてボールを跳ね返し、上のブロックを全部消すゲーム。

ヘビゲーム（Snake Game）

矢印キーでヘビを動かし、エサを食べてどんどん体を長くしていく、30秒で自滅できる超お手軽ゲーム。

クイック・クリック（反射神経）

画面にランダムに現れる的（マト）を、30秒間で何回クリックできるか競うゲーム。

どうやって作るの？
あなたがやることは、Geminiにこう頼むだけです。

「インベーダー、ブロック崩し、ヘビゲーム、クリックゲームの4つを切り替えて遊べる、1ファイルのHTMLコードを作って。デザインは黒背景のレトロなゲームセンター風にして！」

そうすると、Geminiがすべてのゲームの処理を1つのコードにまとめて出力します。
あとはそれをメモ帳などに貼り付けて game.html みたいな名前で保存すれば、ダブルクリックするだけであなたのブラウザが「ミニゲーム集サイト」に早変わりします。

これなら一切頑張る必要はありません。

まずは小手調べに、「インベーダー」と「ブロック崩し」の2つが入ったシンプルなサイトのコードをここで作ってみましょうか？それとも、他に「このゲームも入れてほしい」という軽いものはありますか？

CSSとJSも分けて欲しい。キャラデザインはドットでいいからある程度色つけて欲しい

HTML、CSS、JavaScript（JS）の3つにしっかりファイルを分割し、ご要望通り「インベーダー」「ブロック崩し」「ヘビゲーム」の3つをその場で切り替えてサクッと遊べるレトロゲームセンターサイト（ミニゲーム集）を作成しました！

全体のデザインは、黒と深い緑（少しミリタリーテイストを感じるような渋めのトーン）をベースにした筐体風のデザインに仕上げ、各ゲームのキャラクターやブロックにはカラフルな色付けをしてレトロなドット感を演出しています。

生成されたファイル
HTMLファイル（画面の骨組みとメニュー切り替え）
HTML のアイコン
retro_arcade/index
 HTML 
開く

CSSファイル（レトロゲーム筐体風のデザイン・ネオンカラー配色）
CSS のアイコン
retro_arcade/style
 CSS 
開く

JavaScriptファイル（3つのゲームの動き・当たり判定・描画処理）
JS のアイコン
retro_arcade/game
 JS 
開く

各ゲームの仕様（色付き・ドット絵風）
SPACE INVADERS（インベーダー）

自機は渋いディープグリーン（砲台付き）。迫ってくる敵は行ごとに「ピンク」「オレンジ」「水色」「黄緑」に色分けされ、ドット絵の「目」のような表現を入れてあります。スペースキーで黄色い弾を撃ちます。

BLOCK BREAKOUT（ブロック崩し）

一番上のブロックから赤・オレンジ・黄・緑・水色と綺麗なグラデーションで配置され、ブロックの隙間に黒い縁取りを入れてレトロ感を強調しています。パドルはキーボードのほか、マウスを左右に動かすだけでも追従するお手軽仕様です。

RETRO SNAKE（ヘビゲーム）

真っ赤なリンゴ（エサ）を食べるとヘビがどんどん伸びます。ヘビの頭は明るい黄緑、体は深みのある緑でカクカク動くクラシックな仕様です。

遊び方
上記の3つのファイルをダウンロードし、同じフォルダ（ディレクトリ）に保存します。

HTMLファイルは名前を index.html に、CSSは style.css に、JSは game.js にしてください。

index.html をダブルクリックしてブラウザで開くだけで、専用のゲームセンター画面が起動します。

ボタンを押せばその場ですぐにゲームが始まり、やられたら数秒でメニューに戻って別のゲームを遊べます。

頑張って複雑なものを作るより、こういう「3分で遊んで終われる」ミニゲームが並んでいる方が気楽で楽しいですよね。ぜひ手元で動かして小気味よく遊んでみてください！




Gemini は AI であり、間違えることがあります。

let currentGame = null;
let animationFrameId = null;
const canvas = document.getElementById('gameCanvas');
const ctx = canvas.getContext('2d');
let score = 0;

const keys = {};
window.addEventListener('keydown', e => keys[e.key] = true);
window.addEventListener('keyup', e => keys[e.key] = false);

window.switchGame = function(gameType) {
    document.getElementById('menu-screen').style.display = 'none';
    document.getElementById('game-screen').style.display = 'flex';
    if (animationFrameId) cancelAnimationFrame(animationFrameId);
    score = 0;
    updateScore();
    if (gameType === 'invaders') {
        document.getElementById('game-title').innerText = 'SPACE INVADERS';
        document.getElementById('controls-text').innerText = '操作: [←][→] で移動 / [Space] で弾発射';
        initInvaders();
    } else if (gameType === 'breakout') {
        document.getElementById('game-title').innerText = 'BLOCK BREAKOUT';
        document.getElementById('controls-text').innerText = '操作: [←][→] もしくは マウス移動';
        initBreakout();
    } else if (gameType === 'snake') {
        document.getElementById('game-title').innerText = 'RETRO SNAKE';
        document.getElementById('controls-text').innerText = '操作: 矢印キーでヘビの方向転換';
        initSnake();
    }
}

window.backToMenu = function() {
    if (animationFrameId) cancelAnimationFrame(animationFrameId);
    canvas.onmousemove = null;
    document.getElementById('game-screen').style.display = 'none';
    document.getElementById('menu-screen').style.display = 'flex';
}

function updateScore() {
    document.getElementById('score-display').innerText = 'SCORE: ' + score;
}

// 1. Invaders
let player, bullets, enemies, enemyBullets, enemyDirection, enemySpeed, shootCooldown;
function initInvaders() {
    player = { x: canvas.width / 2 - 15, y: canvas.height - 40, width: 30, height: 20, speed: 4 };
    bullets = []; enemyBullets = []; enemies = []; enemyDirection = 1; enemySpeed = 1.0; shootCooldown = 0;
    const colors = ['#ff3366', '#ff9933', '#33ffff', '#33ff33'];
    for (let r = 0; r < 4; r++) {
        for (let c = 0; c < 8; c++) {
            enemies.push({ x: 60 + c * 55, y: 50 + r * 35, width: 30, height: 20, color: colors[r], points: (4 - r) * 10 });
        }
    }
    gameLoopInvaders();
}

function gameLoopInvaders() {
    if (keys['ArrowLeft'] && player.x > 0) player.x -= player.speed;
    if (keys['ArrowRight'] && player.x < canvas.width - player.width) player.x += player.speed;
    if (shootCooldown > 0) shootCooldown--;
    if (keys[' '] && shootCooldown === 0) {
        bullets.push({ x: player.x + player.width / 2 - 2, y: player.y, width: 4, height: 10, speed: 6 });
        shootCooldown = 20;
    }
    bullets.forEach((b, index) => { b.y -= b.speed; if (b.y < 0) bullets.splice(index, 1); });
    enemyBullets.forEach((eb, index) => {
        eb.y += eb.speed; if (eb.y > canvas.height) enemyBullets.splice(index, 1);
        if (eb.x < player.x + player.width && eb.x + eb.width > player.x && eb.y < player.y + player.height && eb.y + eb.height > player.y) {
            alert('GAME OVER! SCORE: ' + score); backToMenu(); return;
        }
    });
    let hitWall = false;
    enemies.forEach(e => {
        e.x += enemySpeed * enemyDirection;
        if (e.x + e.width > canvas.width - 20 || e.x < 20) hitWall = true;
        if (Math.random() < 0.003) enemyBullets.push({ x: e.x + e.width/2, y: e.y + e.height, width: 4, height: 8, speed: 3 });
    });
    if (hitWall) {
        enemyDirection *= -1;
        enemies.forEach(e => { e.y += 15; if (e.y + e.height >= player.y) { alert('GAME OVER'); backToMenu(); return; } });
    }
    bullets.forEach((b, bIdx) => {
        enemies.forEach((e, eIdx) => {
            if (b.x < e.x + e.width && b.x + b.width > e.x && b.y < e.y + e.height && b.y + b.height > e.y) {
                bullets.splice(bIdx, 1); score += e.points; updateScore(); enemies.splice(eIdx, 1);
            }
        });
    });
    if (enemies.length === 0) { alert('STAGE CLEAR!!'); backToMenu(); return; }
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = '#2e5c46'; ctx.fillRect(player.x, player.y, player.width, player.height);
    ctx.fillRect(player.x + 12, player.y - 6, 6, 6);
    ctx.fillStyle = '#ffff33'; bullets.forEach(b => ctx.fillRect(b.x, b.y, b.width, b.height));
    ctx.fillStyle = '#ff3333'; enemyBullets.forEach(eb => ctx.fillRect(eb.x, eb.y, eb.width, eb.height));
    enemies.forEach(e => {
        ctx.fillStyle = e.color; ctx.fillRect(e.x, e.y, e.width, e.height);
        ctx.fillStyle = '#000'; ctx.fillRect(e.x + 6, e.y + 5, 4, 4); ctx.fillRect(e.x + 20, e.y + 5, 4, 4);
    });
    animationFrameId = requestAnimationFrame(gameLoopInvaders);
}

// 2. Breakout
let ball, paddle, bricks;
function initBreakout() {
    paddle = { x: canvas.width / 2 - 40, y: canvas.height - 30, width: 80, height: 12, speed: 6 };
    ball = { x: canvas.width / 2, y: canvas.height - 50, dx: 3, dy: -3, radius: 6 };
    bricks = [];
    const colors = ['#ff3333', '#ff9933', '#ffff33', '#33ff33', '#33ffff'];
    for (let r = 0; r < 5; r++) {
        for (let c = 0; c < 9; c++) {
            bricks.push({ x: 15 + c * 63, y: 40 + r * 22, width: 55, height: 15, color: colors[r], status: 1 });
        }
    }
    canvas.onmousemove = function(e) {
        const rect = canvas.getBoundingClientRect();
        paddle.x = e.clientX - rect.left - paddle.width / 2;
    };
    gameLoopBreakout();
}
function gameLoopBreakout() {
    if (keys['ArrowLeft'] && paddle.x > 0) paddle.x -= paddle.speed;
    if (keys['ArrowRight'] && paddle.x < canvas.width - paddle.width) paddle.x += paddle.speed;
    if (paddle.x < 0) paddle.x = 0; if (paddle.x > canvas.width - paddle.width) paddle.x = canvas.width - paddle.width;
    ball.x += ball.dx; ball.y += ball.dy;
    if (ball.x + ball.radius > canvas.width || ball.x - ball.radius < 0) ball.dx *= -1;
    if (ball.y - ball.radius < 0) ball.dy *= -1;
    if (ball.y + ball.radius >= paddle.y && ball.x > paddle.x && ball.x < paddle.x + paddle.width) {
        ball.dy = -Math.abs(ball.dy); ball.dx = (ball.x - (paddle.x + paddle.width/2)) * 0.1;
    }
    if (ball.y > canvas.height) { alert('GAME OVER! SCORE: ' + score); backToMenu(); return; }
    let allCleared = true;
    bricks.forEach(b => {
        if (b.status === 1) {
            allCleared = false;
            if (ball.x > b.x && ball.x < b.x + b.width && ball.y > b.y && ball.y < b.y + b.height) {
                ball.dy *= -1; b.status = 0; score += 10; updateScore();
            }
        }
    });
    if (allCleared) { alert('ALL CLEAR!!!'); backToMenu(); return; }
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = '#33ffff'; ctx.fillRect(paddle.x, paddle.y, paddle.width, paddle.height);
    ctx.beginPath(); ctx.arc(ball.x, ball.y, ball.radius, 0, Math.PI * 2); ctx.fillStyle = '#fff'; ctx.fill(); ctx.closePath();
    bricks.forEach(b => {
        if (b.status === 1) { ctx.fillStyle = b.color; ctx.fillRect(b.x, b.y, b.width, b.height); ctx.strokeStyle = '#000'; ctx.strokeRect(b.x, b.y, b.width, b.height); }
    });
    animationFrameId = requestAnimationFrame(gameLoopBreakout);
}

// 3. Snake
let snake, food, dx, dy, gridCount, lastUpdateTime;
function initSnake() {
    gridCount = 20;
    snake = [{ x: 10 * gridCount, y: 10 * gridCount }, { x: 9 * gridCount, y: 10 * gridCount }, { x: 8 * gridCount, y: 10 * gridCount }];
    dx = gridCount; dy = 0; spawnFood(); lastUpdateTime = 0;
    gameLoopSnake(0);
}
function spawnFood() {
    food = {
        x: Math.floor(Math.random() * (canvas.width / gridCount)) * gridCount,
        y: Math.floor(Math.random() * (canvas.height / gridCount)) * gridCount
    };
}
function gameLoopSnake(timestamp) {
    animationFrameId = requestAnimationFrame(gameLoopSnake);
    if (timestamp - lastUpdateTime < 120) return;
    lastUpdateTime = timestamp;
    if (keys['ArrowUp'] && dy === 0) { dx = 0; dy = -gridCount; }
    if (keys['ArrowDown'] && dy === 0) { dx = 0; dy = gridCount; }
    if (keys['ArrowLeft'] && dx === 0) { dx = -gridCount; dy = 0; }
    if (keys['ArrowRight'] && dx === 0) { dx = gridCount; dy = 0; }
    const head = { x: snake[0].x + dx, y: snake[0].y + dy };
    if (head.x < 0 || head.x >= canvas.width || head.y < 0 || head.y >= canvas.height) { alert('GAME OVER! SCORE: ' + score); backToMenu(); return; }
    for (let i = 0; i < snake.length; i++) {
        if (head.x === snake[i].x && head.y === snake[i].y) { alert('GAME OVER'); backToMenu(); return; }
    }
    snake.unshift(head);
    if (head.x === food.x && head.y === food.y) { score += 20; updateScore(); spawnFood(); } else { snake.pop(); }
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = '#ff3333'; ctx.fillRect(food.x + 2, food.y + 2, gridCount - 4, gridCount - 4);
    snake.forEach((part, index) => {
        ctx.fillStyle = index === 0 ? '#33ff33' : '#1e3d2f';
        ctx.fillRect(part.x + 1, part.y + 1, gridCount - 2, gridCount - 2);
    });
}
retro_arcade/game.js
「retro_arcade/game.js」を表示しています。
